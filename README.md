# Fullstack проект - ЛР №2

Лабораторная работа №2: Развёртывание сервера; базовая маршрутизация; наличие health‑check.

## Структура проекта

```
fullstack/
├── backend/              # FastAPI backend
│   ├── app/
│   │   ├── routers/     # Роутеры API
│   │   ├── main.py      # Главный файл приложения
│   │   ├── config.py    # Конфигурация
│   │   └── database.py  # Настройки БД
│   ├── main.py          # Точка входа
│   └── requirements.txt # Зависимости Python
├── frontend/            # React frontend
│   ├── src/
│   │   ├── pages/      # Страницы
│   │   ├── services/   # API сервисы
│   │   └── App.jsx     # Главный компонент
│   └── package.json    # Зависимости npm
└── README.md
```

## Требования

- Python 3.9+
- Node.js 18+
- npm или yarn

## Установка и запуск

### Backend

```bash
cd backend
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
python main.py
```

Backend будет доступен по адресу: http://localhost:8000
API документация: http://localhost:8000/docs

### Frontend

```bash
cd frontend
npm install
npm run dev
```

Frontend будет доступен по адресу: http://localhost:3000

## Реализованные функции

### Backend
- ✅ FastAPI с полной структурой
- ✅ Health-check эндпоинты
- ✅ Модульная архитектура роутеров
- ✅ CORS настройка
- ✅ API для всех модулей приложения:

**Модули:**
- 🔐 Авторизация (регистрация, вход, JWT токены)
- 👤 Профиль и управление членами семьи
- 💊 Каталог лекарств с поиском
- 📅 Календарь приемов (месяц/неделя)
- ⏰ Расписание приема лекарств
- 📄 Экспорт в PDF

### Frontend
- ✅ React + Vite
- ✅ Одна страница с health-check
- ✅ Интеграция с API
- ✅ Минималистичный UI

## API Эндпоинты

Подробная документация по всем эндпоинтам находится в файле [API_ENDPOINTS.md](backend/API_ENDPOINTS.md)

### Основные модули:

1. **Authentication** (`/api/v1/auth`)
   - Регистрация и вход по номеру телефона и паролю
   
2. **Users & Profile** (`/api/v1/users`)
   - Профиль пользователя
   - Управление членами семьи
   - Настройка SMS уведомлений

3. **Medications** (`/api/v1/medications`)
   - Каталог лекарств
   - Поиск по названию
   - CRUD операции

4. **Appointments** (`/api/v1/appointments`)
   - Календарь приемов (неделя/месяц)
   - Статусы приема (предстоит/принял/не принял)
   - Экспорт в PDF

5. **Schedules** (`/api/v1/schedules`)
   - Расписание приема лекарств
   - Периодичность (каждый день/через день)

### Базовые эндпоинты:

- `GET /` - Главная страница API
- `GET /health` - Простая проверка здоровья
- `GET /health/detailed` - Подробная проверка здоровья

## Автор

Лабораторная работа №2
